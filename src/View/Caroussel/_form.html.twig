<form method="post">
    <input type="hidden" value="{{ image.id }}" name="id">
    <input type="hidden" value="{{ image.url }}" name="url">
    <input type="text" value="{{ image.article_id }}" name="article_id">
    <input type="submit" value="Valider">
</form>
